export async function insertAudit(connection,{empresaId,userId,action,entity,entityId,before=null,after=null,requestId=null,ip=null,userAgent=null}){await connection.execute(`INSERT INTO auditoria(empresa_id,usuario_id,acao,entidade,entidade_id,dados_anteriores,dados_novos,ip,user_agent,request_id) VALUES(?,?,?,?,?,?,?,?,?,?)`,[empresaId,userId||null,action,entity,String(entityId),before?JSON.stringify(before):null,after?JSON.stringify(after):null,ip||null,userAgent||null,requestId||null])}
