# Frota Leve — Frontend

A pasta do frontend foi reservada nesta etapa, mas a interface final ainda não será implementada.

Antes de escrever os componentes React, o projeto seguirá a ordem aprovada:

1. wireframes;
2. Design System;
3. estados de tela e acessibilidade;
4. componentes reutilizáveis;
5. implementação React + Vite.

Isso evita transformar novamente as regras do banco em uma interface difícil de usar.
